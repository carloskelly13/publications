import React from "react"
import { LayersSidebarContainer } from "./container"

export const LayersSidebar = ({ visible }) => (
  <LayersSidebarContainer visible={visible}>
    Hello
  </LayersSidebarContainer>
)
